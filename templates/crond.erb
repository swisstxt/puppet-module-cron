<% env.each do |key, value| -%>
<%= key %>=<%= value %>
<% end -%>
<%= [
  Array(minute).join(','),
  Array(hour).join(','),
  Array(day_of_month).join(','),
  Array(month).join(','),
  Array(day_of_week).join(','),
  user, command
  ].join(' ') %>
